# echo-agent semantic baseline

This directory records repository-scoped semantic facts. The baseline remains
open and currently models only the multilingual SDK facade boundary required by
Plan 8. That boundary is one child of the repository model; SDK identity counts
are drift telemetry, not whole-workspace semantic completion. The next expansion
route is whole-workspace semantic discovery.
