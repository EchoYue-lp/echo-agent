# echo-agent semantic baseline

This directory records repository-scoped semantic facts. The initial baseline
is intentionally open and currently models only the multilingual SDK facade
boundary required by Plan 8.
